# ── Synthure ─────────────────────────────────────────────────────────────────
# The demo runs with zero config on a note-derived "offline engine".
# Add an Anthropic key to make the four writer agents + verifier + orchestrator
# real Claude calls (live generation on any note you type).

# For local dev, put this in frontend/.env.local
ANTHROPIC_API_KEY=sk-ant-...
