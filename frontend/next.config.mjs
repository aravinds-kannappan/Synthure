/** @type {import('next').NextConfig} */
const nextConfig = {
  // Synthure runs entirely on Next.js route handlers (see app/api/synthesize).
  // No external backend proxy.
}

export default nextConfig
