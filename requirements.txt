# Core framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-multipart>=0.0.12

# Auth
PyJWT>=2.8.0

# Anthropic AI
anthropic>=0.39.0

# Supabase
supabase>=2.0.0
httpx>=0.27.0

# ML
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.13.0

# HuggingFace dataset ingestion (run once to train denial predictor + readmission index)
datasets>=2.20.0
tqdm>=4.66.0

# sentence-transformers removed: all-MiniLM-L6-v2 embeddings are fetched via
# HF Inference API (HF_TOKEN) so PyTorch is not needed locally.
# This keeps the deployment under Vercel’s 250 MB serverless bundle limit.
