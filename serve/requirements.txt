# torch (CPU) is installed separately in the Dockerfile from the CPU wheel index.
fastapi>=0.110
uvicorn[standard]>=0.29
transformers>=4.44
numpy>=1.24
sentencepiece>=0.1.99   # DeBERTa (faithfulness) tokenizer
